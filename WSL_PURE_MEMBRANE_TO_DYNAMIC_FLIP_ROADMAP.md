# WSL 纯膜静态代码 → 非对称驱动翻转模型更新计划

> 目标：以当前 `shoot_free_arc.py` 纯膜静态打靶代码为基线，逐步发展出可与 COMSOL 对照、可做分岔/路径依赖分析、最终能够描述 **WSL 结构剥离—内翻—翻转动力学** 的 Python 数值模型。

---

## 0. 当前基线与代码定位

当前代码已经完成：

- 二维轴对称 WSL 封闭膜中面几何；
- 正确的几何参数：左侧膜外/内边界 `r=14/15 mm`、膜厚 `1 mm`、左/右直段 `90/120 mm`；
- 上下端 `45° 圆弧 + 21.213 mm 45°斜线 + 135°圆弧`；
- Yeoh 不可压缩超弹性纯膜本构；
- 贴壁段 + 自由膜段的半环打靶求解；
- `s_c`（贴壁材料半弧长）扫描与压力扫描；
- 膜力、主伸长、接触压力、膜应变能、腔体体积等后处理；
- 使用上一步解作为下一步打靶初值的简单延拓能力。

当前模型本质上仍是 **准静态纯膜降阶模型**，主要用于和 COMSOL `para=1` 充压贴壁态做基线对照。

### 当前已知限制

1. `s_c` 仍是外部输入，剥离前沿不是自洽求解结果；
2. 参考材料坐标 `S` 与 COMSOL 实际贴壁材料区间尚未标定；
3. 纯膜模型没有弯曲刚度，无法严格描述剥离边界层、曲率反转和内翻；
4. 接触只通过“预设贴壁段”表示，没有真正的接触搜索、分离和自接触；
5. 没有质量、阻尼、时间积分和惯性；
6. 没有 `p(V)` 封闭气体耦合；
7. 没有主动环/非对称驱动；
8. 没有伪弧长延拓、极限点和稳定性分析；
9. 当前纯膜 + 无黏附 + 切线连续剥离条件存在过定问题，只能在部分参数下得到精确闭解，其余为准闭解。

---

# 1. 总体开发路线

```text
当前纯膜静态基态
      ↓
COMSOL 静态标定与材料坐标对位
      ↓
准静态连续分支 / 伪弧长延拓
      ↓
加入弯曲能与剥离前沿
      ↓
离散多环动力学
      ↓
加入墙面接触 / 分离 / 自接触
      ↓
加入封闭气体 p(V)
      ↓
加入非对称主动环驱动
      ↓
剥离传播 → 内翻 → 翻转
      ↓
与 COMSOL 动态结果交叉验证
```

开发原则：**不要用 Python 重写一个完整 COMSOL；Python 模型主要负责机制解释、分岔、能量、翻转前沿和快速参数扫描。**

---

# 2. Phase A：冻结并整理当前静态基线

## A1. 保留 v7 作为不可修改参考版本

建议：

```text
legacy/
└── shoot_free_arc_v7.py
```

用途：

- 保留当前静态打靶结果；
- 后续每次重构都与 v7 输出对比；
- 防止动力学开发破坏已有静态结果。

## A2. 拆分代码模块

建议目录：

```text
wsl/
├── geometry.py
├── material.py
├── static_shooting.py
├── continuation.py
├── contact.py
├── bending.py
├── actuation.py
├── dynamics.py
├── gas.py
├── metrics.py
└── validation.py
```

### 现有函数迁移建议

- `_build_segments`, `ref_r0`, `ref_z0` → `geometry.py`
- `W1_of`, `yeoh_N`, `inv_lam1` → `material.py`
- `integrate_half`, `solve_fixed_sc` → `static_shooting.py`
- `postprocess` → `metrics.py`

## A3. 增加基础回归测试

至少检查：

- 中面总弧长 `L0 ≈ 349.815 mm`；
- 左/右中面半径 `14.5/60.5 mm`；
- 上下过渡段连续；
- `ref_r0(S)`、`ref_z0(S)` 首尾闭合；
- Yeoh 本构在 `λ1=λ2=1` 附近无异常；
- 固定 `p, s_c` 时重构前后静态结果一致。

---

# 3. Phase B：先完成 COMSOL 静态标定

这是动力学开发前必须完成的步骤。

## B1. 标定材料坐标映射

需要从 COMSOL `para=1` 状态读取：

- 实际接触区起点/终点；
- 接触区对应的未变形材料坐标；
- 贴壁材料长度；
- 变形前后同一材料点的 `(r,z)`。

最终建立：

```text
S ↔ COMSOL material point
```

并取消“贴壁段默认从 `S=0` 开始”的临时假设。

## B2. 统一接触中面半径

当前几何应统一使用：

```text
R_w = 62.0 mm        # 刚性壁
R_c = 61.5 mm        # 膜中面贴壁半径
```

所有中面方程、接触几何和 `λ2` 计算统一使用 `R_c`，避免 `61.5/62 mm` 混用。

## B3. 建立静态对照指标

Python 与 COMSOL 对比：

- 接触长度 `L_c`；
- 总接触力 `F_c`；
- 膜轮廓；
- 总弹性应变能 `U_strain`；
- 腔体体积 `V`；
- 主伸长 `λ_s, λ_φ`；
- 质心位置。

目标不是追求接触边缘点压力峰值完全一致，而是优先保证积分量和整体构型一致。

---

# 4. Phase C：加入真正的连续分支分析

## C1. 把静态平衡统一写成残差形式

```text
R(x, λ) = 0
```

其中 `λ` 可以依次选择：

- 压力参数 `para`；
- 实际压差 `p`；
- 贴壁长度/剥离前沿；
- 主动环驱动幅值 `adrv`。

## C2. 实现伪弧长延拓

新增：

```text
continuation.py
```

目标：

- 穿越普通参数扫描无法通过的 fold/limit point；
- 区分真实多分支与求解器路径依赖；
- 捕获稳定支/不稳定支；
- 复现 COMSOL 正反向加载的迟滞区间。

输出：

```text
para - Lc
para - Fc
para - U
para - V
para - smallest_singular_value(J)
```

## C3. 稳定性诊断

至少增加：

- 雅可比最小奇异值；
- 切线刚度最小特征值（若可获得）；
- fold 点自动标记；
- 分支编号与正/反加载路径标签。

---

# 5. Phase D：从“纯膜”升级到“膜 + 弯曲”

翻转模型必须完成这一阶段。

## D1. 加入弯曲刚度

定义：

```text
D = E h^3 / [12(1-ν^2)]
```

并增加曲率能：

```text
U_b = 1/2 ∫ D (κ - κ0)^2 dA
```

作用：

- 给剥离边界层一个有限长度尺度；
- 允许曲率从正 → 0 → 负；
- 消除纯膜剥离过定问题的一部分；
- 为 snap-through 和 inside-out inversion 提供能垒。

## D2. 数值实现建议

不建议继续使用当前“半环连续 ODE 打靶”作为最终动力学核心。

推荐把中面离散成 `N=20~40` 个环带/节点：

```text
Ring_1 - Ring_2 - ... - Ring_N
```

每个环带保留：

```text
r_i, z_i, ψ_i, λs_i, λφ_i
```

相邻节点之间计算：

- Yeoh 拉伸能；
- 离散曲率；
- 弯曲能。

---

# 6. Phase E：建立接触、分离与剥离前沿

## E1. 第一版：墙面 penalty contact

刚性壁：

```text
r = R_c
```

定义间隙：

```text
g_i = R_c - r_i
```

第一版可使用平滑 penalty：

```text
F_contact = k_c * penetration
```

并加入适当正则化，避免接触开/闭导致积分器抖动。

## E2. 接触状态量

每个环定义：

```text
state_i ∈ {free, contact, peeling, flipped}
```

输出：

- 总接触长度 `L_c(t)`；
- 接触环数量；
- 上/下接触前沿；
- 剥离前沿 `S_peel(t)`。

## E3. 第二版：加入黏附/CZM 或能量剥离判据

可选参数：

```text
G_c     # 断裂/剥离能
T_max   # 最大黏附牵引
```

目标：使 `s_c` 从外部输入变成真正的求解结果。

## E4. 第三版：自接触

翻转过程中如出现膜自身穿透，再加入：

- 环带间最小距离搜索；
- 非相邻节点 self-contact；
- 空间网格/邻居表加速。

短期 MVP 阶段不强制实现。

---

# 7. Phase F：动力学核心

## F1. 加入质量

每个环带质量：

```text
m_i = ρ * V_i
```

方程：

```text
M q¨ + C q˙ + ∂U/∂q = F_pressure + F_contact + F_act + F_gravity
```

## F2. 阻尼

第一版使用小阻尼：

- 质量比例阻尼；或
- Kelvin/viscous 数值阻尼。

必须做阻尼灵敏度测试，保证“是否翻转”不是由阻尼参数人为决定。

## F3. 时间积分

Python 优先：

- `scipy.integrate.solve_ivp(method="Radau")`
- 或 `BDF`

后续若接触非常刚，可实现：

- Newmark-β；
- generalized-α。

## F4. 能量审计

每个时间步记录：

```text
T_kinetic
U_stretch
U_bending
U_gravity
U_contact
W_actuator
E_total
```

真正 snap-through 应体现：

```text
U_elastic ↓  +  T_kinetic ↑
```

---

# 8. Phase G：封闭腔体压力模型

开发顺序必须分层。

## G1. MVP

先固定：

```text
p = 300 Pa
```

优先实现一次可重复的翻转。

## G2. 封闭气体

加入等温：

```text
p(t) V(t) = p0 V0
```

即：

```text
p(t) = p0 * V0 / V(t)
```

## G3. 可选升级

多变过程：

```text
p V^γ = const
```

并允许外压/背压显式存在。

---

# 9. Phase H：非对称主动环驱动

这部分直接服务于“短期实现翻转”。

## H1. 不建议最终使用绝对指定位移

COMSOL 中可以暂时使用 Prescribed Displacement 做触发验证；Python 模型优先使用：

- 主动应变；
- 主动自然长度；
- 主动首选曲率。

## H2. 单环驱动 MVP

选择上端一段材料区间：

```text
S ∈ [Sa, Sb]
```

定义主动应变：

```text
ε_act(S,t) = -ε0 * a(t)
```

或自然长度缩短：

```text
L0_active = L0 * (1 - α a(t))
```

时间函数：

```text
加载 → 保持 → 卸载
```

卸载后若结构继续翻转，则说明执行器只是帮助系统跨越能垒。

## H3. 多环顺序驱动

按 Hou 式控制环思想：

```text
A1 → A2 → A3 → ...
```

定义：

```text
a_i(t) = a(t - i Δt)
```

用于研究：

- 剥离前沿传播；
- 旅行波式内翻；
- 最小驱动环数量；
- 最小驱动能量。

---

# 10. Phase I：翻转判据与动力学输出

不能只靠动画判断“翻了”。

## I1. 局部法向反转

定义：

```text
χ_i(t) = n_i(t) · n_i(0)
```

若：

```text
χ_i < 0
```

则该局部已发生 inside-out inversion。

## I2. 翻转前沿

定义：

```text
S_flip(t)
```

并计算：

```text
dS_flip/dt
```

## I3. 重心和力矩

输出：

```text
r_G(t), z_G(t)
```

相对瞬时支撑/接触点：

```text
M_G = (r_G - r_C) × m g
```

用于验证：

```text
接触重分配 → 重心滞后 → 力矩建立 → 剥离/翻转
```

## I4. 主要输出量

- `L_c(t)`
- `F_c(t)`
- `S_peel(t)`
- `S_flip(t)`
- `r_G(t), z_G(t)`
- `M_G(t)`
- `U_stretch(t)`
- `U_bending(t)`
- `T_kinetic(t)`
- `p(t), V(t)`
- actuator work

---

# 11. Phase J：与 COMSOL 的双向验证

## J1. 静态验证

优先匹配：

```text
轮廓
Lc
Fc
U_strain
V
COM
```

不把接触边缘 `q_max` 作为首要拟合指标。

## J2. 路径依赖验证

COMSOL 已观察到：

- 正向/反向加载结果不同；
- `para≈0.67` 附近存在接触长度跃迁；
- 总接触力积分量比局部峰值更稳定。

Python 需要复现：

```text
正向分支
反向分支
fold/limit point
迟滞区间
```

## J3. 动态验证

比较：

- 翻转是否发生；
- 翻转临界驱动幅值；
- 剥离前沿时间历程；
- 特征点轨迹；
- 翻转时间；
- 最大速度；
- 能量释放过程。

---

# 12. 推荐短期 MVP：先在 Python 中实现一次翻转

为了尽快得到结果，不等待所有高级物理完成。

## MVP 模型

```text
20~40 个轴对称环带
+ Yeoh 拉伸
+ 离散弯曲能
+ 固定 p=300 Pa
+ 刚性壁 penalty contact
+ 小阻尼
+ 惯性
+ 上端单环主动收缩
```

暂时不加：

- CZM；
- 摩擦；
- 自接触；
- 完整气体热力学；
- 多环复杂控制。

## MVP 成功判据

至少满足：

1. 驱动侧开始剥离；
2. `S_peel(t)` 连续传播；
3. 一个或多个环 `χ_i<0`；
4. 卸载执行器后仍继续内翻；
5. 能量曲线显示弹性能向动能转化；
6. 数值结果对时间步和环数量基本收敛。

---

# 13. 建议代码版本规划

```text
v7   当前纯膜静态打靶基线
v8   COMSOL 材料坐标映射 + RC 统一 + 回归测试
v9   伪弧长延拓 + 分支/稳定性诊断
v10  多环离散 + 弯曲能
v11  动力学 + 墙面接触
v12  单环非对称驱动 + 首次翻转 MVP
v13  p(V) + 重心/力矩 + 能量闭合
v14  多环顺序驱动
v15  CZM/摩擦/自接触 + COMSOL 全量验证
```

---

# 14. 下一步立即执行的开发任务

- [ ] 将当前 `shoot_free_arc.py` 固化为 `legacy/shoot_free_arc_v7.py`
- [ ] 新建 `wsl/geometry.py`
- [ ] 新建 `wsl/material.py`
- [ ] 增加几何/本构回归测试
- [ ] 从 COMSOL 导出 `para=1` 的真实接触材料区间
- [ ] 校准 `S ↔ COMSOL` 映射
- [ ] 统一 `R_c=61.5 mm`
- [ ] 重构静态求解接口为 `R(x, λ)=0`
- [ ] 实现伪弧长延拓
- [ ] 建立 `20~40` 环多环离散几何
- [ ] 加入离散弯曲能
- [ ] 加入质量、阻尼与时间积分
- [ ] 加入刚性壁 penalty contact
- [ ] 加入单环主动收缩驱动
- [ ] 输出 `S_peel`, `S_flip`, `COM`, `M_G`, 能量分解
- [ ] 实现一次可重复的卸载后自主翻转
- [ ] 回到 COMSOL 做动态交叉验证

---

# 15. 最终研究定位

最终不追求“Python 替代 COMSOL”，而采用双模型路线：

```text
COMSOL：高保真几何 / 超弹性 / 接触 / 自接触 / 应力场 / 动态验证

Python：降阶动力学 / 多环模型 / 分岔 / 能量 / 剥离前沿 / 重心力矩 / 快速扫参
```

最终需要回答的核心机理问题是：

```text
充气预应力如何建立？
        ↓
接触状态为什么发生路径依赖？
        ↓
局部非对称驱动如何触发剥离？
        ↓
剥离前沿如何传播？
        ↓
弹性能如何转化成动能？
        ↓
重心和瞬时支撑点如何形成翻转力矩？
        ↓
什么参数决定 WSL 能否完成自主翻转？
```
